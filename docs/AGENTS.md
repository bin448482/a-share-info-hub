# docs 目录指令

本目录遵循仓库根目录 `AGENTS.md` 的全部规则；这里不重复根规则全文，只记录文档目录的补充约束。

## 适用范围

- 本目录用于保存项目实施计划、数据契约、验证报告和用户可 review 的说明文档。
- 文档必须区分“计划”“已实测结果”和“待验证假设”，不能把未运行的接口探测写成事实。
- 实施计划应明确成功标准、阻断条件、输出文件和验证方式。
- 面向 review 的文档优先说明业务边界和取舍，不混入无关实现细节。
